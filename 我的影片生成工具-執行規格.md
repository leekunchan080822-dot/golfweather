# 我的影片生成工具（仿 NextFrame 風格）  

> 目標：做一個給自己用的中文操作流程，像平台一鍵一樣，輸入一句需求就能產影片。

## 1. 想做的事（MVP）

1. 我只要輸入一句話需求（`brief`）  
2. 系統自動補齊參數：風格、語速、時長、配音、構圖  
3. 點擊執行後，產生：
   - `video_id`
   - 狀態（pending / completed / failed）
   - `video_url`（成功）  
4. 自動記錄輸出結果到一份清單（方便追蹤）

## 2. 工具架構（模仿她的核心邏輯）

- **前端輸入層**：只放三個欄位
  - `brief`（中文需求）
  - `style`（風格）
  - `duration`（秒數）
- **自動化引擎**
  - 腳本規則化（把自然語言轉為正式腳本）
  - Avatar 驗證（預設 ID + 可用清單比對）
  - 產生參數包（payload）
  - 呼叫 HeyGen `v3/videos`
  - 輪詢 `v3/videos/{video_id}`
- **輸出層**
  - 產生結果、影片網址、失敗原因
  - 下載 Artifact

## 3. 中文欄位設計（給你真的好用）

### 必填
- `brief`：影片主題/腳本需求  
- `title`：標題（若空則自動給 `default-YYYYMMDD`）

### 選填
- `avatar_id`：空值就走 Secret
- `duration_sec`：預設 10
- `resolution`：預設 `1080p`
- `aspect_ratio`：預設 `16:9`
- `dry_run`：`true/false`，預先只做檢查不生成

## 4. 建議的「一句話需求」模板（中文）

- `請幫我做一則 [場景] 的10秒短片，台詞是「[腳本]」，口吻要[風格]，要有自然生活感。`
- 舉例：  
  `請幫我做一則淡江大橋直播掉手機進淡水河的10秒短片，台詞台灣年輕男生口吻，語氣要自然，畫面偏紀實風。`

## 5. 流程（你每次只做這 3 步）

1. 開 `Generate HeyGen影片（自動）` Workflow  
2. 填 `brief`，確認 `duration_sec`（10/15/20）與 `dry_run`  
3. 送出後看 Log：
   - 顯示 `avatar 驗證通過`
   - 顯示 `video_id`
   - 最後顯示 `Video ready: ...`

## 6. 成功輸出標準

- Log 有 `video_url=...`
- Artifacts 有 `*.mp4`
- 失敗時必有：
  - `avatar_not_found` 或 `insufficient credit` 或 `failure_message`

## 7. 失敗對策（快速修復）

- `avatar_not_found`  
  - 用 list workflow 抓可用 id，複製正確 id 再試
- `insufficient credit`  
  - 檢查 HeyGen API credits，先做網頁試片再補點數
- `no video_id`  
  - 看 `HTTP`/`error` 原始訊息，先重跑一次 dry_run
- 長時間 pending  
  - 等 1~2 分鐘再重新看 log，或調整成 `resolution=720p`

## 8. 這份工具要怎麼像平台一樣做得好看

你可以先用「文字版 UI」：
- 一張卡片放 `brief`
- 下方放三個切換：`style`、`duration`、`dry_run`
- 右側顯示「流程狀態」與「最後輸出網址」

## 9. 下一步（可直接實作）

1. 在這份規格上做一個前端頁面（HTML）
2. 把 workflow inputs 換成這 5 個欄位
3. 加上結果日誌面板（成功/失敗訊息＋影片網址）  

---

我建議先照這份規格先跑，等你穩定後再加前端（React/Next）視覺化頁面。
